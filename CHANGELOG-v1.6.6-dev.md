# v1.6.6-dev

- Fix XHS library comparison path to scan `work_path/folder_name`, such as `/xhs/Download`, instead of only `/xhs`.
- Fix XHS liked/collected name comparison so it submits the full loaded list instead of stopping at the incremental-download cutoff title.
