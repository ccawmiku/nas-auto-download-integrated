# v1.6.5-dev

- XHS error list now supports single-item retry and batch retry from the web page.
- XHS userscript can submit liked/collected note names for local folder comparison, and link submissions now include note metadata when available.
- Integrated service forwards XHS library comparison requests to the worker and mounts the XHS download directory into the main container.
- Douyin job rows no longer show the editable like/collection name and mode boxes between the checkbox and URL.
