# v1.7.1-dev

- Make XHS single retry submit the failed item URL back to the worker so stale note ids or old records are requeued correctly.
- Use `/volume2/se-p/ins` as the default Instagram download mount in compose and docs.
