# CHANGELOG v2.0.0

## 统一控制台

- 使用独立的 React + TypeScript + Vite 前端，重新设计为浅色现代 SaaS 控制台。
- 增加可折叠左侧导航、服务状态卡片、任务入口、日志视图和基础移动端查看布局。
- 统一服务只提供状态和导航，四个平台的详细任务与配置页面继续由各自 Worker 维护。

## 后端边界

- 将平台 Worker 的状态查询和手动运行抽成独立适配器注册表。
- 小红书和抖音 f2 继续保持独立进程、独立配置和独立状态文件，方便后续跟随上游或 fork 更新。
- 保留 `14001` 统一入口、`13001` 小红书 API 和现有平台代理路径。

## 构建与验证

- Dockerfile 增加固定版本 Node 构建阶段，最终镜像仍使用现有 Python/Playwright 运行时。
- 本地验证使用前端 TypeScript 构建、Python 单元测试和语法检查；未在本机运行 Docker。
