# v1.7.0-dev

- Merge current `dev` work into `main`.
- Add first Instagram browser-to-Docker workflow using a userscript collector and a Docker-side `yt-dlp` queue worker.
- Add Instagram service entry, config/state/queue/download mounts, and `/api/instagram/links` receiver.
