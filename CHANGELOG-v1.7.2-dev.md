# v1.7.2-dev

- Split Instagram downloading by media type: `gallery-dl` handles images/carousels and `yt-dlp` handles videos/Reels.
- Save Instagram downloads into one folder per post ID instead of grouping by author.
- Update the Instagram userscript to also match `instagram.com` without `www`.
