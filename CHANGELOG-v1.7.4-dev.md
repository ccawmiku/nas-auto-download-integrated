# v1.7.4-dev

- 删除上一版外站实验集成、worker 源码、userscript、依赖和 compose 挂载。
- 小红书 worker 按 XHS-Downloader 上游 API/日志结果类型识别失败，覆盖 `获取数据失败`、`提取数据失败`、`下载失败`、`请求失败`、`格式判断失败` 等情况，避免接口 200 时误判成功。
- 小红书页面删除手动批量粘贴链接框和目录校验区域；统一入口与 userscript 也移除目录校验 API/按钮。
- 抖音页面新增“最大运行时长（秒）”配置，默认 300 秒，作为连续跳过判断失效时的单项目停止后备。
