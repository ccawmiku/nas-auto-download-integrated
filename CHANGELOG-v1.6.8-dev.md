# v1.6.8-dev

- XHS library comparison results now keep missing item URLs so the userscript can send missing notes directly to Docker.
- The userscript comparison modal now lists missing local notes and adds a one-click "send missing to Docker" action.
