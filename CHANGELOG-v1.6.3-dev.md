# v1.6.3-dev

- 移除统一首页 Cookie 导入和 cookies.txt 文件上传入口，Cookie 改为各项目页面手动粘贴保存。
- 统一首页改为运行总览，显示各项目服务状态、运行状态、当前任务和下次运行倒计时；服务入口只保留在侧边栏。
- 移除 X 全局浏览器锁机制，只保留项目自身运行锁。
- 小红书新增红色“清空所有队列”按钮、单独错误列表，以及 `ReadTimeout`、`RemoteProtocolError`、`peer closed`、`网络异常` 等临时网络错误的延迟重试。
- 抖音 f2 子任务取消固定单任务超时，改为连续 10 个作品全为 `[跳过]` 时停止当前点赞/收藏项目。
